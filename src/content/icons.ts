import { Brain, Cpu, Database, GitBranch, Gauge, Layers, Network, Plug, Server, Shield } from "lucide-react";
import type { IconName } from "./types";

export const ICONS: Record<IconName, typeof Brain> = {
  Brain,
  Plug,
  Database,
  Network,
  Shield,
  Gauge,
  Server,
  Layers,
  Cpu,
  GitBranch,
};
